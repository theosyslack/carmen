{
  "name": "carmen",
  "version": "0.0.1",
  "description": "Carmen will sneak around a website, finding precious elements and returning them to you.",
  "main": "dist/index.js",
  "license": "MIT",
  "dependencies": {
    "chalk": "^2.4.1",
    "p-queue": "^3.0.0",
    "puppeteer": "^1.10.0"
  },
  "scripts": {
    "start": "node dist/index.js",
    "watch-build": "nodemon -q dist/index.js",
    "watch-src": "babel --quiet --watch src -d dist",
    "develop": "concurrently npm:watch-*",
    "build": "babel src -d dist"
  },
  "devDependencies": {
    "@babel/core": "^7.1.2",
    "@babel/preset-env": "^7.1.0",
    "babel-cli": "^6.26.0",
    "babel-polyfill": "^6.26.0",
    "babel-preset-latest": "^6.24.1",
    "concurrently": "^4.0.1",
    "nodemon": "^1.18.5",
    "npm-run-all": "^4.1.3"
  }
}